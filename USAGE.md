# 📖 使用指南：如何添加新论文

## 🎯 快速上手

添加新论文只需要 **3 个步骤**：

### 步骤 1️⃣：打开 `script.js` 文件

在项目文件夹中找到 `script.js` 文件，用任何文本编辑器打开（记事本、VS Code、Sublime Text 等）。

### 步骤 2️⃣：找到 `papersData` 数组

在文件最开头，你会看到这样的代码：

```javascript
const papersData = [
    {
        title: "基于深度学习的图像识别算法优化研究",
        authors: [ ... ],
        ...
    },
    // 更多论文...
];
```

### 步骤 3️⃣：添加你的论文数据

在数组中添加新的论文对象（记得在前一个对象后面加逗号）：

```javascript
const papersData = [
    // ... 已有的论文 ...
    {
        title: "你的论文标题",
        authors: [
            { name: "你的名字", isCorresponding: true, isFirstAuthor: false },
            { name: "合作者", isCorresponding: false, isFirstAuthor: true }
        ],
        journal: "发表的期刊或会议名称",
        date: "2024年12月",
        level: { text: "SCI一区", color: "purple" },
        tags: ["标签1", "标签2", "标签3"],
        links: [
            { icon: "📄", text: "PDF", url: "#" },
            { icon: "💻", text: "代码", url: "#" }
        ]
    }
];
```

保存文件，刷新浏览器，新论文就会自动显示！

---

## 📋 字段详细说明

### 1. `title` - 论文标题
```javascript
title: "基于深度学习的图像识别算法优化研究"
```
- 类型：字符串
- 必填项
- 显示在卡片顶部的论文标题

### 2. `authors` - 作者列表
```javascript
authors: [
    { name: "张三", isCorresponding: true, isFirstAuthor: false },
    { name: "李四", isCorresponding: false, isFirstAuthor: true },
    { name: "Renye Zhang", isCorresponding: false, isFirstAuthor: false }
]
```
- 类型：数组
- 必填项
- 每个作者是一个对象，包含：
  - `name`: 作者姓名
  - `isCorresponding`: 是否为通讯作者
    - `true`: 会在姓名后添加 `*` 标记
    - `false`: 不添加标记
  - `isFirstAuthor`: 是否为共同第一作者
    - `true`: 会在姓名后添加 `#` 标记
    - `false`: 不添加标记
  - **特别说明**：如果作者姓名为 `Renye Zhang`，会自动用紫色加粗显示（无论是否为通讯作者）

### 3. `journal` - 期刊/会议名称
```javascript
journal: "IEEE Transactions on Pattern Analysis and Machine Intelligence"
```
- 类型：字符串
- 必填项
- 发表的期刊或会议全称

### 4. `date` - 发表时间
```javascript
date: "2024年3月"
```
- 类型：字符串
- 必填项
- 建议格式：`年份+月份`

### 5. `level` - 论文级别
```javascript
level: { text: "SCI一区", color: "purple" }
```
- 类型：对象
- 必填项
- 包含两个字段：
  - `text`: 显示的文字（如：SCI一区、CCF A类、核心期刊、顶刊等）
  - `color`: 标签颜色，可选值：
    - `"purple"` - 紫色（推荐用于SCI一区、顶刊）
    - `"yellow"` - 黄色（推荐用于CCF A类）
    - `"green"` - 绿色（推荐用于核心期刊）
    - `"blue"` - 蓝色（推荐用于SCI二区）

### 6. `tags` - 关键词标签
```javascript
tags: ["机器学习", "深度学习", "计算机视觉"]
```
- 类型：数组
- 必填项
- 可以添加任意数量的标签
- 标签会自动配色（紫、蓝、粉、绿等颜色循环）

### 7. `links` - 资源链接
```javascript
links: [
    { icon: "📄", text: "PDF", url: "https://example.com/paper.pdf" },
    { icon: "💻", text: "代码", url: "https://github.com/username/repo" },
    { icon: "📊", text: "数据", url: "#" }
]
```
- 类型：数组
- 必填项（可以是空数组 `[]`）
- 每个链接包含：
  - `icon`: 图标（使用 emoji）
    - 常用图标：📄（PDF）、💻（代码）、📊（数据）、🔗（链接）、📖（文档）
  - `text`: 链接显示的文字
  - `url`: 链接地址
    - 如果暂时没有链接，可以填 `"#"`

---

## 🌟 完整示例

### 示例 1：有完整资源的论文
```javascript
{
    title: "Attention Is All You Need",
    authors: [
        { name: "Vaswani", isCorresponding: true, isFirstAuthor: false },
        { name: "Shazeer", isCorresponding: false, isFirstAuthor: false },
        { name: "Parmar", isCorresponding: false, isFirstAuthor: false }
    ],
    journal: "NeurIPS 2017",
    date: "2017年12月",
    level: { text: "CCF A类", color: "yellow" },
    tags: ["Transformer", "注意力机制", "NLP"],
    links: [
        { icon: "📄", text: "PDF", url: "https://arxiv.org/pdf/1706.03762.pdf" },
        { icon: "💻", text: "代码", url: "https://github.com/tensorflow/tensor2tensor" },
        { icon: "📊", text: "数据", url: "https://example.com/dataset" }
    ]
}
```

### 示例 2：有共同第一作者的论文
```javascript
{
    title: "我的研究论文",
    authors: [
        { name: "Renye Zhang", isCorresponding: false, isFirstAuthor: true },  // Renye Zhang 会紫色显示，# 标记
        { name: "合作者", isCorresponding: false, isFirstAuthor: true },       // 共同第一作者，# 标记
        { name: "导师", isCorresponding: true, isFirstAuthor: false }          // 通讯作者，* 标记
    ],
    journal: "某期刊",
    date: "2024年",
    level: { text: "核心期刊", color: "green" },
    tags: ["研究方向"],
    links: [
        { icon: "📄", text: "PDF", url: "https://example.com/paper.pdf" }
    ]
}
```

### 示例 3：暂无资源链接的论文
```javascript
{
    title: "即将发表的论文",
    authors: [
        { name: "张三", isCorresponding: true, isFirstAuthor: false },
        { name: "李四", isCorresponding: false, isFirstAuthor: false }
    ],
    journal: "投稿中",
    date: "2024年",
    level: { text: "顶刊", color: "purple" },
    tags: ["AI", "机器学习"],
    links: []  // 空数组表示暂无链接
}
```

---

## ⚠️ 常见错误

### 1. 忘记添加逗号
```javascript
// ❌ 错误 - 缺少逗号
const papersData = [
    { title: "论文1", ... }
    { title: "论文2", ... }  // 这里会报错
];

// ✅ 正确
const papersData = [
    { title: "论文1", ... },  // 注意这里有逗号
    { title: "论文2", ... }
];
```

### 2. 引号不匹配
```javascript
// ❌ 错误
title: "我的论文'

// ✅ 正确
title: "我的论文"
```

### 3. 布尔值用了引号
```javascript
// ❌ 错误
{ name: "张三", isCorresponding: "true", isFirstAuthor: "false" }  // true 和 false 不应该加引号

// ✅ 正确
{ name: "张三", isCorresponding: true, isFirstAuthor: false }
```

---

## 💡 技巧提示

1. **批量添加**：可以一次性添加多篇论文，保存后一起显示
2. **排序**：数组中的顺序就是页面显示的顺序，最新的论文放在最前面
3. **备份**：修改前建议先备份 `script.js` 文件
4. **测试**：每次修改后刷新浏览器，检查是否正常显示

---

## 🆘 遇到问题？

如果页面显示空白或出错：

1. **检查浏览器控制台**：
   - 按 F12 打开开发者工具
   - 切换到 Console 标签
   - 查看是否有红色错误信息

2. **常见错误提示**：
   - `Unexpected token` - 语法错误，检查逗号、引号、括号是否匹配
   - `Cannot read property` - 某个字段缺失，检查是否所有必填项都填写了

3. **恢复初始状态**：
   - 如果改错了，可以重新下载原始的 `script.js` 文件

---

## 🎉 完成！

现在你已经掌握了如何轻松添加新论文。每次发表新论文时，只需要：

1. 打开 `script.js`
2. 在 `papersData` 数组最前面添加新论文数据
3. 保存并刷新页面

就这么简单！✨
